# RELEASE-PREP WAVE — work report

Base: `e134bcf` (post root-cleanup). Audit: `audit/release_readiness.md` (now committed). JUCE: 9.0.1 at `~/JUCE` (not a git checkout — tag `9.0.1` assumed for CI clone; README/CI both pin it).

## Per-item changes

1. **NOTICES.md** — Added "Mutable Instruments Eurorack DSP (clouds/rings/warps/stmlib, MIT)" section: per-repo URL table mapping each vendored tree to the FX it feeds, copyright form taken verbatim from file headers (© 2012–2015 Emilie Gillet, MIT notice retained in-file), and the documented localised patches (dsp.h portability, SRC circular fast-path removal, quadrature phase clamp, frac16FromQ12). Rewrote the JUCE section: modules are dual-licensed **AGPLv3/commercial** (per ~/JUCE/LICENSE.md; ISC applies to bundled deps/examples, not the modules), Parvati consumes them under AGPLv3. Also (same file, licensing-correctness): added **clap-juce-extensions (MIT, Paul Walker)** — it is compiled into the CLAP binary and the old "Other libraries" section wrongly claimed JUCE+stdlib only; marked the Unifont section as **no longer embedded** (font-mode selector was removed; `fonts/` files remain in-tree, `grep unifont Source/ CMakeLists.txt` = 0 hits).
2. **presets/ATTRIBUTION.md** — Fixed the false "Parvati is therefore itself licensed under GPL-3.0" sentence: presets are GPL-3.0, plugin is AGPL-3.0, GPL→AGPL one-way compatibility, both FOSS, notice + corresponding-source obligations stated; links to root LICENSE/NOTICES added.
3. **CMakeLists.txt** — (a) deleted `JUCE_DISPLAY_SPLASH_SCREEN=0`, one-line comment explains JUCE 9 ignores it; verified no splash warnings in a fresh full reconfigure. (b) `CMAKE_OSX_DEPLOYMENT_TARGET=13.0` for non-iOS Apple, guarded-empty+FORCE (CMake pre-seeds an empty cache entry that silently defeats a plain `set(CACHE)` — first attempt proved this: minos stayed 26.0). Verified `otool LC_BUILD_VERSION → minos 13.0` (SDK 26.5). (c) Version grep: only `CMakeLists.txt:4 VERSION 0.1.0` → `PARVATI_VERSION` define → editor labels (PluginEditor.cpp:3029, 4150; `0.0.0` fallback at :31 is a compile guard only). **Bump point is line 4 alone.**
4. **README.md** — Tuning section rewritten (raga presets only + removal note incl. backcompat: old states/presets load, fall back to 12-EDO); Features bullet fixed; new **Requirements** section (macOS 13+/arm64, x86_64 dropped with universal-build flag, iOS 14+ AUv3 iPad-landscape context, JUCE 9.0.1 at ~/JUCE); JUCE clone instructions 9.0.0→9.0.1; quarantine note reframed (ad-hoc local builds) + pointer to the release script. Grep clean of stale Scala/custom-table claims.
5. **CHANGELOG.md** — No edit: the two `ios_device_checklist.md` references are valid path pointers; the surrounding prose is historical narrative (correct as history). [Unreleased] left intact per instructions.
6. **audit/ios_device_checklist.md** — D4 refreshed: open-in handling is IMPLEMENTED (`IosOpenIn.{h,mm}`, headless-pinned by `ios_openin_test`); `.scl/.kbm` removed everywhere; UTI count 5→3 with exact ids; added the negative check (a .scl must no longer offer Parvati) and a failure-triage pointer (security-scoped URL read).
7. **tools/release/** — NEW `sign_and_notarize.sh` (executable, bash -n clean; shellcheck not installed) + `README.md` + `macos_release.entitlements` (disable-library-validation, JUCE-documented for plugin hosting). Signs inner Mach-Os then bundles (no `--deep` for signing), verifies `--deep --strict`, notarytool submit --wait + stapler staple/validate, ditto-packaged dist zip **including README+NOTICES** (AGPL compliance), spctl assess. Credentials via env: `DEV_ID` + one of `PARVATI_NOTARY_PROFILE` / ASC API key trio (recommended, App Store Connect key requirement documented) / Apple-ID trio; `SKIP_NOTARIZE=1` for sign-only. Failure-path smoke-tested: missing/bogus DEV_ID → exit 1, no dist dir created. NOT run end-to-end (no credentials — by design).
8. **.github/workflows/ci.yml** — NEW; python-yaml lint OK. macOS 15 arm64 runner; JUCE 9.0.1 via actions/cache + guarded clone with version grep assert; Release configure (arm64, CLAP on); builds VST3/AU/CLAP/Standalone; runs an 11-binary representative subset (parvati_tests, apvts, host_param_text, tuning, preset, patch_load, roundtrip_golden, fixed_math, transport_clock, note_stack, render_quality — full-suite-is-118 note in comments); `parvati_static_checks`; ditto artefact zips + upload@v4; concurrency-cancel; push/PR on main.
9. Commit: **one commit** `chore(release): notarization/licensing/CI/README release prep` — **not pushed** (per instructions). Includes the previously-untracked `audit/release_readiness.md` (README references it).

## Verification
- Rebuild sanity: `cmake --build build -j8 --target Parvati Parvati_VST3` → `[100%] Built target`, no splash warnings, minos 13.0 confirmed.
- YAML lint passed; `bash -n` passed; `.gitignore` root-md guard does not affect `tools/release/README.md`.

## REMAINS for the user (cannot be done without credentials/decisions)
- Developer ID cert + ASC key; first real run of `tools/release/sign_and_notarize.sh`.
- Release version number: bump `CMakeLists.txt:4`, cut `[Unreleased]` → versioned section, git tag.
- iOS: team id, App ID/App-Group registration, `ios/exportOptions.plist`, .ipa path (BLOCKER 2).
- Manual QA: `auval -v aumu Prvt 805L`, VST3 `validator`, pluginval ≥5, DAW smoke matrix (Logic AU / Live+Reaper VST3 / Bitwig CLAP / Standalone), `audit/ios_device_checklist.md` D1–D8 on device.
- Release notes must call out arm64-only + the documented non-fixes (Spectral 64 ms latency, ClockedDelay glide, sub-32k AA, undo-on-part-switch).
- First CI run needs pushing `main` (workflow only runs on GitHub).
