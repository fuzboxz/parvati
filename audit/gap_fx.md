# FX Test-Coverage Gap Audit — Source/dsp/fx/**

## Prioritized missing tests (max 6)

1. **`tailSecondsForFx` WSOLA freeze + engine-cache multi-part/BPM staging** — unit `tailSecondsForFx`/`SynthEngine::recomputeTailCache` (FxTypes.h:234-318, SynthEngine.cpp:2384-2410). Pin: WSOLAStretch `param[3]>0.5` → `kTailCapSeconds` (only LoopingDelay p3 and Spectral p[4] are pinned — render_quality_test.cpp:232-240); `Phaser/Chorus/Gate/etc → 0.0` (only None pinned, :162); two parts with different tails → cache reports the MAX over parts (recomputeTailCache loops all parts — only one part/slot is ever exercised, render_quality_test.cpp:253-279); host BPM change re-triggers recompute so a ClockedDelay tail shrinks (tempo path untested at cache level). Target: extend `parvati_render_quality_test` §3b/§3d. Effort **S**.

2. **Clouds FX at host rate ≤ 32 kHz** — unit `HostRateBridge::prepare/process` (HostRateBridge.h:39-64) via real processors. All Clouds-effect tests run 44.1/48/96k (parvati_clouds_fx_test.cpp:348, fx_routing, param_coverage); at ≤32 kHz `aaActive_=false` (no AA/recon Svf cascade) AND `ratio_>1` — the internal-upsample branch where `m > n` and the `m<=0` ZOH branch shape differs. Only the *separate* fv1 `RateBridge` class is tested at 22.05k (fv1_engine_test.cpp:152-171). Pin: Diffuser/Reverb/LoopingDelay finite + non-silent + differs at {22050, 32000}. Target: extend `parvati_clouds_fx_test` (`CloudsFxHostRateSub32k`). Effort **S/M**.

3. **Chain `latency()` mixed slots + bypass invariance (N1/N2)** — unit `FxChain::latency`/`delayPassthrough` (FxChain.h:139-146). Only single-OS-slot == 8 is pinned (fx_param_coverage_test.cpp:531-543). Pin: Series OS+OS+OS → 24; OS + latency-0 slot → 8; parallel with one OS branch → 8; **latency() unchanged across `setSlotEnabled(false)`** (the stated N1 invariant) and a bypassed OS slot's passthrough stays latency-delayed (no output snap, N2). Target: extend `parvati_fx_param_coverage_test` testLatency. Effort **S**.

4. **Topology permutations with FV-1 / buffer-based effects + tailing slot in the parallel sum** — unit `FxChain::process`/`renderParallel`. All topology×order sweeps use only Diffuser/Reverb/PitchShifter (fx_routing_test.cpp:78-116, fx_param_coverage_test.cpp:576-578). Pin: (a) Echo/Plate/Spring survive Parallel topologies with finite wet; (b) a *just-bypassed* slot (enabled=false, wetFade_>5e-4) still contributes AND still divides the `activeCount` equal-gain sum — render a two-slot parallel branch, bypass one mid-stream, assert the survivor's gain doesn't jump (activeCount re-derivation). Target: extend `parvati_fx_routing_test`. Effort **M**.

5. **Mid-session sample-rate change re-prepare** — unit `FxChain::prepare` (FxChain.h:92-98) + processors. B7 (fx_routing_test.cpp:255-276) re-prepares at the SAME 48000/256; no test re-prepares at a different rate (e.g. 48k→96k→44.1k) with enabled slots, staged-but-unconsumed swaps, and asserts continuity: finite output, wetFade/dryWetCur preserved, bridge re-armed (AA filters switch on/off at the 32k boundary), no stale-rate audio replay from delay rings (clearDelayRings on install). Target: extend `parvati_fx_routing_test` (B7b). Effort **S**.

6. **`FxChain::setTempo` → ClockedDelay host-BPM sync through the chain** — unit `FxChain::setTempo` (FxChain.h:118-120) + `SynthEngine.cpp:1848`. ClockedDelay tempo tracking is only tested processor-direct via `setTransport` (fv1_clocked_delay_test.cpp:158-188); the chain push path (engine AudioPlayHead → fxChains_[p].setTempo → slot override; no-op for other types) is never exercised. Pin: chain-level Echo-peak position halves when BPM doubles via `chain.setTempo`, and `setTempo` on non-ClockedDelay slots changes nothing (bit-identical output). Target: extend `fv1_clocked_delay_test` (chain section). Effort **S**.

## Already covered
- All 24 non-None types: factory+`type()`, finite @ {0, 0.5, 1}, full-wet differs, per-param sweep active/inert — `fx_param_coverage_test`.
- FV-1 family behavior + 0/1 param corners — `fv1_{clocked_delay,ensemble,phaser,plate_reverb,vinyl_compressor,newfamily,engine}_test`.
- 3×6 topology×order finite+wet, dry bypass, master mix/EQ extremes, tail fade-out, engage fade-in, D1/D2 OS latency blends — `fx_routing_test`, `fx_param_coverage_test`.
- HostRateBridge 980 Hz sub-chunk cadence, pop/click, 1-sample m==0 ZOH dropout, 44.1/48/96k — `parvati_fx_{bridge_pop,tinychunk,modrate,engine_continuity}`.
- Engine onset/chord/sustain click-free render for every type — `parvati_fx_onset_regression`; Spectral/Resonator L/R balance — `parvati_fx_stereo_balance_test`.
- Tail table: reverbs (Plate/Spring/Room/CVerb law/Diffuser), Echo feedback law + 0.995 sentinel, ClockedDelay tempo+clamp+bpm-fallback, LoopingDelay/Spectral freeze, clamps (floor/cap/NaN), cache floor→plate→disable→echo — `render_quality_test` §3.
- FX mod matrix all 18 destinations @980 Hz, AC/DC coupling, per-voice sources — `fx_modrate`, `fx_voice_mod`, `fx_param_coverage` §8; oversized-block chunking + offline 8x OS — `render_quality_test` §1-2.
