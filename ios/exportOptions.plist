<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      exportOptions.plist for the `deploy_ios` CMake target
      (CMakeLists.txt → xcodebuild -exportArchive).

      TEMPLATE — edit before a REAL .ipa export:
        method        development | app-store | ad-hoc | enterprise
        teamID        your Apple Developer team id (same as
                      -DPARVATI_IOS_DEVELOPMENT_TEAM=XXXXXXXXXX at configure)
        signingStyle  automatic | manual

      With signingStyle=automatic and a registered App ID + App Group
      (group.com.805labs.parvati), xcodebuild selects a matching provisioning
      profile. For TestFlight / App Store, set method=app-store. Bitcode is off
      (removed in Xcode 16; the AUv3/app targets set ENABLE_BITCODE NO).
    -->
    <key>method</key>
    <string>development</string>
    <key>teamID</key>
    <string>YOUR_TEAM_ID</string>
    <key>signingStyle</key>
    <string>automatic</string>
    <key>compileBitcode</key>
    <false/>
    <key>uploadBitcode</key>
    <false/>
    <key>uploadSymbols</key>
    <false/>
</dict>
</plist>
