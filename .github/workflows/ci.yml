name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

# Cancel superseded runs on the same ref.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # AU is macOS-only; on Windows/Linux JUCE builds Standalone + VST3.
        os: [macos-latest, ubuntu-latest, windows-latest]

    steps:
      - name: Checkout Parvati
        uses: actions/checkout@v4

      # Several tests (patch / multi_load / roundtrip / parvati_tests) load the
      # ORIGINAL Ambika factory files from ambika_reference/ as a bit-exactness
      # oracle. That tree is git-ignored (it is the GPL-3.0 reference firmware
      # with its own history), so CI clones it shallowly here. Its data files
      # live in the main repo, so no submodules are needed.
      - name: Fetch Ambika reference tree (for oracle tests)
        run: git clone --depth 1 https://github.com/pichenettes/ambika.git ambika_reference

      - name: Checkout JUCE
        uses: actions/checkout@v4
        with:
          repository: juce-framework/JUCE
          # Pin to a stable release tag (matches the locally-developed version).
          # Bump deliberately — never use the moving default branch in CI.
          ref: 9.0.0
          path: JUCE

      - name: Install JUCE Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libasound2-dev libx11-dev libxcomposite-dev libxcursor-dev \
            libxext-dev libxinerama-dev libxrandr-dev libxrender-dev \
            libfreetype6-dev libfontconfig1-dev libgl1-mesa-dev libwebkit2gtk-4.1-dev

      - name: Configure (Debug)
        run: cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug -DJUCE_GLOBAL_PATH=${{ github.workspace }}/JUCE

      - name: Build
        run: cmake --build build --config Debug -j 4

      - name: Run tests
        shell: bash
        run: |
          status=0
          # Match the test binaries on every OS: unix has no extension, Windows
          # appends .exe. Iterate both patterns and skip anything that doesn't
          # exist. NOTE: 'parvati_tests' does NOT match 'parvati_*_test', so it is
          # listed explicitly (a bare 'parvati_*_test' glob silently skips it).
          for t in build/parvati_tests build/parvati_tests.exe \
                   build/parvati_*_test build/parvati_*_test.exe; do
            [ -e "$t" ] || continue
            name=$(basename "$t")
            if "$t" > "test_$name.log" 2>&1; then
              echo "::group::$name PASS"
              echo "$name: PASS"
            else
              echo "::group::$name FAIL"
              echo "$name: FAIL (rc=$?)"
              status=1
            fi
            cat "test_$name.log"
            echo "::endgroup::"
          done
          exit $status

      - name: Upload test logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-logs-${{ matrix.os }}
          path: test_parvati_*.log
          if-no-files-found: ignore
